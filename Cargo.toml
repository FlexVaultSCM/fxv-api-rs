[package]
name = "fxv-api"
version = "0.3.0"
edition = "2024"
description = "Public API, types, and shared local-filesystem conventions for working with FlexVault"
license = "MIT"
repository = "https://github.com/FlexVaultSCM/fxv-api-rs"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
thiserror = "2.0.17"
directories = "6.0.0"

[dev-dependencies]
jsonschema = "0.28"
